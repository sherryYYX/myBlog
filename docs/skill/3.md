## 数组扁平化

```javaScript
二维数组扁平化
let arr = [[0, 1], [2, 3], [4, 5]]

let result =  arr.reduce((acc, item, index) => {
  const flatten = acc.concat(item);
  return flatten;
});
console.log(result); // [ 0, 1, 2, 3, 4, 5 ]
```